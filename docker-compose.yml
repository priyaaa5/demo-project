version: '3'

services:
  dev-my-react-app:
    image: priy8808/dev-frontend:1
    container_name: my-react-app-dev
    ports:
      - "3000:3000"
    links:
      - dev-my-db

  dev-my-backend:
    image: priy8808/dev-backend:1
    container_name: my-backend-dev
    environment:
      - APP_ENV=test
    ports:
      - "4006:4006"
    links:
      - dev-my-db

  dev-my-db:
    image: priy8808/dev-db:1
    container_name: dev-db
    ports:
      - "3306:3306"
